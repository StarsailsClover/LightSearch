# LightSearch - 重构版

> 简洁优雅的聚合搜索引擎 | A Simple & Elegant Search Aggregator

[![License: GPL v3](https://img.shields.io/badge/License-GPLv3-blue.svg)](https://www.gnu.org/licenses/gpl-3.0)
[![Version](https://img.shields.io/badge/version-2.0.0-green.svg)](https://github.com/StarsailsClover/LightSearch)

## ✨ 新特性

### 🎯 核心改进
- **完全重构的代码架构** - 采用模块化设计，BEM命名规范
- **弹窗系统重构** - 解决了UI错位和异常问题
- **统一布局模型** - 全面使用Flex/Grid，告别布局混乱

### 🌍 多语言支持
- 自动检测用户语言（基于浏览器/IP）
- 支持语言：简体中文、English、日本語、한국어、Русский
- 用户可手动切换语言
- 完整的i18n系统

### 🎨 主题系统
- **LightSearch Classic** - 经典白色主题
- **Classic Dark** - 深色主题
- **Liquid Glass** - 玻璃态主题（灵感来自 bergice/liquidglass）
- **Eye Comfort** - 护眼绿主题
- 支持跟随系统主题

### ⚡ 新增功能
- **Command Palette** - 按 `Ctrl+K` / `Cmd+K` 快速访问所有功能
- **搜索对比模式** - 在同一窗口对比多个搜索引擎结果
- **弹窗化设置** - 设置和学术搜索均已弹窗化
- **搜索历史增强** - 更智能的历史记录管理
- **引擎管理优化** - 支持启用/禁用引擎

### 🔒 隐私增强
- 本地存储所有数据
- 可选的追踪器拦截
- 随机User-Agent支持

## 📁 项目结构

```
LightSearch/
├── index-new.html          # 新版主页
├── src/
│   ├── app.js             # 主应用程序
│   ├── core/              # 核心模块
│   │   ├── i18n.js        # 国际化系统
│   │   ├── popup.js       # 弹窗管理器
│   │   ├── theme.js       # 主题管理器
│   │   ├── commandPalette.js  # 命令面板
│   │   └── searchEngine.js    # 搜索引擎管理
│   └── styles/
│       └── main.css       # 主样式表（BEM规范）
├── locales/               # 语言包
│   ├── zh.json
│   ├── en.json
│   ├── ja.json
│   ├── ko.json
│   └── ru.json
└── README-REFACTOR.md     # 本文档
```

## 🚀 快速开始

### 方式一：直接使用
1. 打开 `index-new.html`
2. 开始搜索！

### 方式二：本地服务器（推荐）
```bash
# 使用Python
python -m http.server 8000

# 或使用Node.js
npx http-server -p 8000
```

然后访问 `http://localhost:8000/index-new.html`

## 📖 使用指南

### 基础搜索
1. 在搜索框输入关键词
2. 按 `Enter` 或点击"搜索"按钮
3. 所有启用的搜索引擎将在新标签页打开

### 学术搜索
1. 点击右上角"学术搜索"按钮
2. 输入学术关键词
3. 可选择发表时间过滤
4. 支持对比模式

### Command Palette
- 按 `Ctrl+K` (Windows/Linux) 或 `Cmd+K` (Mac)
- 输入命令或搜索
- 使用方向键导航，`Enter` 执行

### 主题切换
- 打开设置 → 个性化 → 主题
- 选择你喜欢的主题
- 支持自定义背景图片

### 语言切换
- 打开设置 → 语言
- 选择你的语言
- 界面立即更新

### 搜索对比模式
1. 打开设置 → 高级 → 启用对比模式
2. 执行搜索时，结果将在同一窗口的标签页中打开
3. 方便对比不同搜索引擎的结果

## 🔧 技术栈

- **纯原生技术** - 无框架依赖
- **ES6 模块** - 现代化的模块系统
- **CSS变量** - 动态主题支持
- **BEM命名** - 可维护的CSS架构
- **LocalStorage** - 本地数据持久化

## 🎯 命名规范

### CSS类名（BEM）
```css
.ls-组件名                /* Block */
.ls-组件名__元素名        /* Element */
.ls-组件名--修饰符        /* Modifier */
```

### 状态类
```css
.is-active               /* 激活状态 */
.is-hidden               /* 隐藏状态 */
.has-error               /* 错误状态 */
```

### JavaScript钩子
```css
.js-功能名               /* 仅供JS使用，CSS不碰 */
```

## 🐛 问题修复

### 已修复的问题
✅ UI错位 - 统一使用Flex/Grid布局  
✅ 弹窗异常 - 重构弹窗系统，使用opacity+pointer-events  
✅ 功能异常 - 模块化重构，清晰的依赖关系  
✅ 命名混乱 - 统一BEM命名规范  
✅ 语法问题 - ES6模块化，严格模式  

## 📝 开发指南

### 添加新语言
1. 在 `locales/` 创建 `{语言代码}.json`
2. 复制 `en.json` 的结构
3. 翻译所有字段
4. 在 `i18n.js` 的 `getSupportedLanguages()` 中添加

### 添加新主题
1. 在 `theme.js` 的 `themes` 对象中添加
2. 定义颜色变量
3. 可选：添加特殊效果（如玻璃态）

### 添加新命令
```javascript
commandPalette.registerCommand({
    id: 'my-command',
    category: 'settings',
    name: () => '我的命令',
    keywords: ['关键词1', '关键词2'],
    action: () => {
        // 执行操作
    }
});
```

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

### 贡献指南
1. Fork 本仓库
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启 Pull Request

## 📄 许可证

本项目基于 [GNU General Public License v3.0](LICENSE) 开源。

## 🙏 致谢

- [bergice/liquidglass](https://github.com/bergice/liquidglass) - Liquid Glass主题灵感来源
- 所有贡献者和用户

## 📮 联系方式

- GitHub: [@StarsailsClover](https://github.com/StarsailsClover)
- Issues: [提交问题](https://github.com/StarsailsClover/LightSearch/issues)

---

**Made with ❤️ by Sails**
