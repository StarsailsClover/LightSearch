/*
LightSearch - 增强版样式表
Copyright (C) 2025 Sails
遵循GNU GPLv3许可证
*/

/* ========== CSS变量系统 ========== */
/* 定义全局CSS变量，用于统一控制样式属性 */
:root {
    /* 颜色 */
    --ls-bg: #ffffff;
    --ls-text: #000000;
    --ls-accent: #4285f4;
    --ls-card-bg: #ffffff;
    --ls-border: #ddd;
    --ls-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
    
    /* 间距 */
    --ls-spacing-xs: 4px;
    --ls-spacing-sm: 8px;
    --ls-spacing-md: 16px;
    --ls-spacing-lg: 24px;
    --ls-spacing-xl: 32px;
    
    /* 圆角 */
    --ls-radius-sm: 4px;
    --ls-radius-md: 8px;
    --ls-radius-lg: 12px;
    --ls-radius-xl: 24px;
    
    /* 过渡 */
    --ls-transition-fast: 0.15s ease;
    --ls-transition-normal: 0.25s ease;
    --ls-transition-slow: 0.4s ease;
    
    /* 毛玻璃效果 */
    --ls-blur: 0px;
    
    /* Z-index */
    --ls-z-base: 1;
    --ls-z-dropdown: 100;
    --ls-z-overlay: 900;
    --ls-z-popup: 1000;
}

/* ========== 全局重置 ========== */
/* 重置所有元素的默认样式，确保跨浏览器一致性 */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

html {
    font-size: 16px;
}

/* 主体样式定义，设置页面基本布局和过渡效果 */
body {
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', 'Helvetica Neue', Arial, sans-serif;
    background-color: var(--ls-bg);
    color: var(--ls-text);
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    align-items: center;
    transition: background-color var(--ls-transition-normal), color var(--ls-transition-normal);
    position: relative;
    overflow-x: hidden;
}

/* 背景层样式，用于显示背景图像或视频 */
#backgroundLayer {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: -1;
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
}

#backgroundLayer::after {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    backdrop-filter: blur(var(--ls-blur));
    -webkit-backdrop-filter: blur(var(--ls-blur));
}

#backgroundVideo {
    width: 100%;
    height: 100%;
    object-fit: cover;
}

/* ========== 搜索区域 ========== */
/* 搜索容器样式，居中显示搜索组件 */
.ls-search-container {
    margin-top: 15vh;
    text-align: center;
    width: 90%;
    max-width: 800px;
    z-index: var(--ls-z-base);
}

/* Logo样式，包含打字机动画效果 */
.ls-logo {
    font-size: 3rem;
    font-weight: 300;
    margin-bottom: var(--ls-spacing-lg);
    color: var(--ls-text);
    overflow: hidden;
}

/* Logo 打字机动画 */
.ls-logo.typing::after {
    content: '|';
    animation: blink 0.7s infinite;
}

@keyframes blink {
    0%, 50% { opacity: 1; }
    51%, 100% { opacity: 0; }
}

/* 搜索框容器样式，支持响应式布局 */
.ls-search-box {
    display: flex;
    gap: var(--ls-spacing-sm);
    margin: var(--ls-spacing-lg) 0;
    flex-wrap: wrap;
    justify-content: center;
}

/* 搜索输入框样式，具有圆角和悬停效果 */
.ls-search-input {
    flex: 1;
    min-width: 300px;
    max-width: 600px;
    padding: 15px 20px;
    border: 2px solid var(--ls-border);
    border-radius: var(--ls-radius-xl);
    font-size: 1.1rem;
    background-color: var(--ls-card-bg);
    color: var(--ls-text);
    transition: border-color var(--ls-transition-fast), box-shadow var(--ls-transition-fast);
}

.ls-search-input:focus {
    outline: none;
    border-color: var(--ls-accent);
    box-shadow: 0 0 0 3px rgba(66, 133, 244, 0.1);
}

/* 搜索按钮样式，包含悬停和激活效果 */
.ls-search-btn {
    padding: 15px 30px;
    border: none;
    border-radius: var(--ls-radius-xl);
    background-color: var(--ls-accent);
    color: white;
    font-size: 1.1rem;
    font-weight: 500;
    cursor: pointer;
    transition: background-color var(--ls-transition-fast), transform var(--ls-transition-fast);
}

.ls-search-btn:hover {
    background-color: #357ae8;
    transform: translateY(-1px);
}

.ls-search-btn:active {
    transform: translateY(0);
}

/* ========== 搜索历史 ========== */
/* 搜索历史容器样式 */
.ls-history {
    margin: var(--ls-spacing-lg) 0;
    padding: var(--ls-spacing-md);
    min-height: 40px;
}

/* 历史项目样式，可点击并具有悬停效果 */
.ls-history__item {
    display: inline-block;
    margin: 4px;
    padding: 8px 16px;
    background-color: rgba(66, 133, 244, 0.1);
    border: 1px solid rgba(66, 133, 244, 0.2);
    border-radius: var(--ls-radius-xl);
    font-size: 0.9rem;
    cursor: pointer;
    transition: all var(--ls-transition-fast);
    color: var(--ls-text);
}

.ls-history__item:hover {
    background-color: rgba(66, 133, 244, 0.2);
    border-color: var(--ls-accent);
    transform: translateY(-1px);
}

/* ========== 控制按钮 ========== */
/* 控制按钮容器样式，固定在右上角 */
.ls-controls {
    position: fixed;
    top: var(--ls-spacing-lg);
    right: var(--ls-spacing-lg);
    display: flex;
    gap: var(--ls-spacing-sm);
    flex-wrap: wrap;
    justify-content: flex-end;
    z-index: var(--ls-z-base);
}

/* 通用按钮样式 */
.ls-btn {
    padding: 10px 16px;
    border: none;
    border-radius: var(--ls-radius-md);
    background-color: var(--ls-card-bg);
    color: var(--ls-text);
    font-size: 0.9rem;
    cursor: pointer;
    box-shadow: var(--ls-shadow);
    transition: background-color var(--ls-transition-fast), transform var(--ls-transition-fast);
}

.ls-btn:hover {
    background-color: #f5f5f5;
    transform: translateY(-1px);
}

.ls-btn:active {
    transform: translateY(0);
}

/* ========== 弹窗系统 ========== */
/* 弹窗遮罩层样式，覆盖整个视口 */
.ls-popup-overlay {
    position: fixed;
    inset: 0;
    background-color: rgba(0, 0, 0, 0.5);
    z-index: var(--ls-z-overlay);
    opacity: 0;
    pointer-events: none;
    transition: opacity var(--ls-transition-normal);
}

.ls-popup-overlay.is-active {
    opacity: 1;
    pointer-events: auto;
}

/* 弹窗主体样式，居中显示并包含滚动 */
.ls-popup {
    position: fixed;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%) scale(0.95);
    background-color: var(--ls-card-bg);
    border-radius: var(--ls-radius-lg);
    box-shadow: 0 10px 40px rgba(0, 0, 0, 0.2);
    max-width: 90vw;
    max-height: 85vh;
    width: 700px;
    overflow: hidden;
    z-index: var(--ls-z-popup);
    opacity: 0;
    pointer-events: none;
    transition: opacity var(--ls-transition-normal), transform var(--ls-transition-normal);
    display: flex;
    flex-direction: column;
}

.ls-popup.is-active {
    opacity: 1;
    pointer-events: auto;
    transform: translate(-50%, -50%) scale(1);
}

/* 弹窗可拖动 */
.ls-popup__header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: var(--ls-spacing-lg);
    border-bottom: 1px solid var(--ls-border);
    cursor: move;
    user-select: none;
    background-color: var(--ls-card-bg);
}

.ls-popup__header:active {
    cursor: grabbing;
}

.ls-popup__title {
    font-size: 1.5rem;
    font-weight: 500;
    color: var(--ls-text);
}

.ls-popup__close {
    width: 32px;
    height: 32px;
    border: none;
    background: none;
    font-size: 1.5rem;
    cursor: pointer;
    color: var(--ls-text);
    opacity: 0.6;
    transition: opacity var(--ls-transition-fast);
}

.ls-popup__close:hover {
    opacity: 1;
}

.ls-popup__body {
    padding: var(--ls-spacing-lg);
    overflow-y: auto;
    flex: 1;
}

.ls-popup__footer {
    padding: var(--ls-spacing-lg);
    border-top: 1px solid var(--ls-border);
    display: flex;
    justify-content: flex-end;
    gap: var(--ls-spacing-sm);
    background-color: var(--ls-card-bg);
}

/* ========== 设置面板 ========== */
/* 设置面板各部分样式 */
.ls-settings__section {
    margin-bottom: var(--ls-spacing-xl);
}

.ls-settings__section-title {
    font-size: 1.1rem;
    font-weight: 500;
    margin-bottom: var(--ls-spacing-md);
    color: var(--ls-text);
}

.ls-settings__item {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: var(--ls-spacing-md) 0;
    border-bottom: 1px solid rgba(0, 0, 0, 0.05);
}

.ls-settings__item:last-child {
    border-bottom: none;
}

.ls-settings__label {
    font-size: 0.95rem;
    color: var(--ls-text);
    flex: 1;
}

.ls-settings__control {
    display: flex;
    align-items: center;
    gap: var(--ls-spacing-sm);
}

/* 开关按钮 */
.ls-switch {
    position: relative;
    display: inline-block;
    width: 50px;
    height: 26px;
}

.ls-switch input {
    opacity: 0;
    width: 0;
    height: 0;
}

.ls-switch__slider {
    position: absolute;
    cursor: pointer;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background-color: #ccc;
    transition: 0.3s;
    border-radius: 26px;
}

.ls-switch__slider:before {
    position: absolute;
    content: "";
    height: 20px;
    width: 20px;
    left: 3px;
    bottom: 3px;
    background-color: white;
    transition: 0.3s;
    border-radius: 50%;
}

.ls-switch input:checked + .ls-switch__slider {
    background-color: var(--ls-accent);
}

.ls-switch input:checked + .ls-switch__slider:before {
    transform: translateX(24px);
}

/* 滑块 */
.ls-slider {
    width: 100%;
    max-width: 200px;
    height: 6px;
    border-radius: 3px;
    background: #ddd;
    outline: none;
    -webkit-appearance: none;
}

.ls-slider::-webkit-slider-thumb {
    -webkit-appearance: none;
    appearance: none;
    width: 18px;
    height: 18px;
    border-radius: 50%;
    background: var(--ls-accent);
    cursor: pointer;
}

.ls-slider::-moz-range-thumb {
    width: 18px;
    height: 18px;
    border-radius: 50%;
    background: var(--ls-accent);
    cursor: pointer;
    border: none;
}

/* 颜色选择器 */
.ls-color-picker {
    display: flex;
    gap: var(--ls-spacing-sm);
    flex-wrap: wrap;
}

.ls-color-item {
    width: 40px;
    height: 40px;
    border-radius: var(--ls-radius-sm);
    border: 2px solid var(--ls-border);
    cursor: pointer;
    transition: transform var(--ls-transition-fast), box-shadow var(--ls-transition-fast);
    position: relative;
}

.ls-color-item:hover {
    transform: scale(1.1);
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.2);
}

.ls-color-item.is-active::after {
    content: '✓';
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    color: white;
    font-size: 1.2rem;
    font-weight: bold;
    text-shadow: 0 0 3px rgba(0, 0, 0, 0.5);
}

.ls-color-item--custom {
    border: 2px dashed var(--ls-border);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.5rem;
    color: var(--ls-text);
    opacity: 0.6;
}

.ls-color-item--custom:hover {
    opacity: 1;
}

/* ========== 表单元素 ========== */
/* 输入框样式 */
.ls-input {
    width: 100%;
    padding: 10px 12px;
    border: 1px solid var(--ls-border);
    border-radius: var(--ls-radius-sm);
    font-size: 0.95rem;
    background-color: var(--ls-card-bg);
    color: var(--ls-text);
    transition: border-color var(--ls-transition-fast);
}

.ls-input:focus {
    outline: none;
    border-color: var(--ls-accent);
}

/* 选择框样式 */
.ls-select {
    padding: 8px 12px;
    border: 1px solid var(--ls-border);
    border-radius: var(--ls-radius-sm);
    background-color: var(--ls-card-bg);
    color: var(--ls-text);
    cursor: pointer;
    font-size: 0.95rem;
}

/* 复选框样式 */
.ls-checkbox {
    width: 18px;
    height: 18px;
    cursor: pointer;
}

/* ========== 引擎列表 ========== */
/* 引擎列表容器样式，支持滚动 */
.ls-engine-list {
    max-height: 250px;
    overflow-y: auto;
}

/* 单个引擎项目样式 */
.ls-engine-item {
    display: flex;
    align-items: center;
    padding: var(--ls-spacing-md);
    margin-bottom: var(--ls-spacing-sm);
    background-color: rgba(0, 0, 0, 0.02);
    border-radius: var(--ls-radius-sm);
    gap: var(--ls-spacing-sm);
}

.ls-engine-item__name {
    font-weight: 500;
    min-width: 100px;
    color: var(--ls-text);
}

.ls-engine-item__url {
    flex: 1;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
    color: #666;
    font-size: 0.85rem;
}

.ls-engine-item__delete {
    padding: 6px 12px;
    border: none;
    background-color: #ff4444;
    color: white;
    border-radius: var(--ls-radius-sm);
    font-size: 0.8rem;
    cursor: pointer;
    transition: background-color var(--ls-transition-fast);
}

.ls-engine-item__delete:hover {
    background-color: #cc0000;
}

/* ========== 按钮样式 ========== */
/* 主要按钮样式 */
.ls-btn--primary {
    background-color: var(--ls-accent);
    color: white;
}

.ls-btn--primary:hover {
    background-color: #357ae8;
}

/* 次要按钮样式 */
.ls-btn--secondary {
    background-color: #6c757d;
    color: white;
}

.ls-btn--secondary:hover {
    background-color: #5a6268;
}

/* ========== 暗黑模式 ========== */
/* 暗黑模式主题样式 */
body.theme-dark {
    --ls-bg: #121212;
    --ls-text: #ffffff;
    --ls-card-bg: #1e1e1e;
    --ls-border: #333;
    --ls-shadow: 0 2px 8px rgba(255, 255, 255, 0.1);
}

/* 暗黑模式输入框修复 */
body.theme-dark input[type="text"],
body.theme-dark textarea,
body.theme-dark select,
body.theme-dark .ls-input,
body.theme-dark .ls-select {
    background-color: var(--ls-card-bg);
    color: var(--ls-text);
    border-color: var(--ls-border);
}

body.theme-dark .ls-btn:hover {
    background-color: #2a2a2a;
}

body.theme-dark .ls-engine-item {
    background-color: rgba(255, 255, 255, 0.05);
}

body.theme-dark .ls-engine-item__url {
    color: #aaa;
}

/* ========== Liquid Glass 主题 ========== */
/* Liquid Glass 主题样式 */
body.theme-liquid-glass {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
}

body.theme-liquid-glass .ls-popup,
body.theme-liquid-glass .ls-search-input,
body.theme-liquid-glass .ls-btn,
body.theme-liquid-glass .ls-engine-item {
    background: rgba(255, 255, 255, 0.1);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.18);
    color: white;
}

body.theme-liquid-glass .ls-text,
body.theme-liquid-glass .ls-logo,
body.theme-liquid-glass .ls-search-input,
body.theme-liquid-glass .ls-btn,
body.theme-liquid-glass .ls-settings__label {
    color: white;
}

body.theme-liquid-glass .ls-search-input::placeholder {
    color: rgba(255, 255, 255, 0.7);
}

/* ========== 响应式设计 ========== */
/* 移动端适配样式 */
@media (max-width: 768px) {
    .ls-popup {
        width: 95vw;
        max-height: 90vh;
    }
    
    .ls-controls {
        width: 100%;
        right: 0;
        padding: 0 var(--ls-spacing-md);
    }
    
    .ls-search-input {
        min-width: 100%;
    }
}

/* ========== 工具类 ========== */
/* 隐藏元素工具类 */
.is-hidden {
    display: none !important;
}

/* 禁用状态工具类 */
.is-disabled {
    opacity: 0.5;
    pointer-events: none;
}

/* ========== 滚动条样式 ========== */
/* 自定义滚动条样式 */
::-webkit-scrollbar {
    width: 8px;
    height: 8px;
}

::-webkit-scrollbar-track {
    background: transparent;
}

::-webkit-scrollbar-thumb {
    background: rgba(0, 0, 0, 0.2);
    border-radius: 4px;
}

::-webkit-scrollbar-thumb:hover {
    background: rgba(0, 0, 0, 0.3);
}

body.theme-dark ::-webkit-scrollbar-thumb {
    background: rgba(255, 255, 255, 0.2);
}

body.theme-dark ::-webkit-scrollbar-thumb:hover {
    background: rgba(255, 255, 255, 0.3);
}